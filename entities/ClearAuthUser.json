{
  "type": "object",
  "properties": {
    "company_id": {
      "type": "string"
    },
    "company_name": {
      "type": "string"
    },
    "role": {
      "type": "string",
      "enum": [
        "driver",
        "approver",
        "company"
      ]
    },
    "full_name": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "password_hash": {
      "type": "string"
    },
    "invite_token": {
      "type": "string"
    },
    "invite_used": {
      "type": "boolean"
    },
    "status": {
      "type": "string",
      "enum": [
        "invited",
        "active",
        "disabled"
      ]
    },
    "allowed_approvers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of approver user IDs this driver can request from"
    },
    "allow_all_approvers": {
      "type": "boolean",
      "description": "If true, driver can request from any approver in the company"
    },
    "department": {
      "type": "string",
      "description": "Department or group this user belongs to, e.g. Night Shift, Site A, HGV Team"
    }
  },
  "required": [],
  "name": "ClearAuthUser"
}