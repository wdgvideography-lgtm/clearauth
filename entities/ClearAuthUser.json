{
  "type": "object",
  "properties": {
    "company_id": {
      "type": "string"
    },
    "company_name": {
      "type": "string"
    },
    "role": {
      "type": "string",
      "enum": [
        "driver",
        "approver",
        "company",
        "employee"
      ]
    },
    "full_name": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "password_hash": {
      "type": "string"
    },
    "invite_token": {
      "type": "string"
    },
    "invite_used": {
      "type": "boolean"
    },
    "status": {
      "type": "string",
      "enum": [
        "invited",
        "active",
        "disabled"
      ]
    },
    "allowed_approvers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of approver user IDs this employee can request from"
    },
    "allow_all_approvers": {
      "type": "boolean",
      "description": "If true, employee can request from any approver in the company"
    },
    "department": {
      "type": "string",
      "description": "Department or group this user belongs to"
    },
    "phone": {
      "type": "string",
      "description": "User's phone number"
    },
    "photo_url": {
      "type": "string",
      "description": "URL to user's profile photo"
    }
  },
  "required": [],
  "name": "ClearAuthUser"
}